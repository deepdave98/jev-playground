# TypeSafe AI, for Jev. https://www.typesafe.ai/
TYPESAFE_API_KEY=

# Optional. If set, the Claude side can go direct to the API instead of
# shelling out to the `claude` CLI, which gives cleaner latency numbers.
# See week-01-lead-triage/src/claude_runner.py for why the CLI needs
# calibrating when this is absent.
ANTHROPIC_API_KEY=
